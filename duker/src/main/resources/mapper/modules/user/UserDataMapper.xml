<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duker.core.mapper.UserDataMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.duker.core.entity.UserDataEntity">
        <id column="user_id" property="userId" />
        <result column="title" property="title" />
        <result column="user_name" property="userName" />
        <result column="user_password" property="userPassword" />
        <result column="user_regdate" property="userRegdate" />
        <result column="user_lastlogin" property="userLastlogin" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        user_id, title, user_name, user_password, user_regdate, user_lastlogin
    </sql>

</mapper>
